services:
  bot:
    build: .
    container_name: discord_claim_bot
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json
    env_file:
      - .env
    # tty/stdin_open can sometimes interfere with logging on some hosts
    tty: false
    stdin_open: false
